# QA Report — TMB 2027 PRD v2.0 개정 사이클

> 실행일 2026-09-17 · 대상: 9 feature (itinerary-core, day-detail, budget-view, today-and-safety, booking-tracker, offline-pwa, route-visuals, packing-checklist, trip-journal) · 게이트 95 · 결과 **QA_PASS**

## 실행 결과
| 단계 | 명령 | 결과 |
|---|---|---|
| L1 타입 | `npx tsc --noEmit` | 0 오류 |
| L1 단위 | `npx vitest run` | 14 files · 113/113 통과 |
| 빌드 | `npx next build` | 성공 (19 routes, Middleware 포함) |
| L2/L3 E2E | `npx playwright test --workers=1` (mobile-360 · tablet-768 · desktop-1440) | 99/99 통과 (2026-09-17 dev 서버) · 2026-09-18부터 프로덕션 빌드(3100, `.next-e2e`)로 실행 — 결과는 아래 재실행 절 |

## E2E 커버리지
| spec | 검증 |
|---|---|
| `responsive.spec.ts` | FR-007 가로 스크롤 0·44px·axe(6 페이지 × 3 뷰포트), FR-008 홈 지표 163.0 km·9,750 m·9,725 m, SC-003 예산 합계·권장 금액·USD/EUR, FR-011 지도 12구간·고개 마커 12·OpenFreeMap 출처, FR-005 Day 1 아침 이동 2구간 |
| `day-detail.spec.ts` | SC-001 홈→Day→숙박 연락 2탭, FR-004 숙소 미정(Day 12)·booking.com 링크 없음, FR-017 원어 표기·음차 0건 |
| `offline.spec.ts` | FR-013/SC-006 캐시 재진입·오프라인 배너·마지막 예약 상태 배지 |
| `packing.spec.ts` | FR-015/SC-011 로컬 저장 유지 |
| `security.spec.ts` | SC-007 admin API 401/403/503·private 필드 0건·bundle에 service_role 없음, 8.3 방문자에게 편집 패널 없음, 8.4 제네바·취리히 후보 호텔, FR-016 기록 공개 열람·이메일 미노출·비로그인 폼 없음 |

## 병렬 실행 참고
기본 `--workers=2` 실행에서는 5초 타임아웃 기반 일부 케이스(SC-001·packing·security 1건)가 뷰포트 3종 병렬 부하로 간헐 실패했다(단독 재실행 시 통과). `--workers=1`로 결정적 통과를 확인했다. CI에서는 `workers: 1` 또는 타임아웃 상향을 권장한다.

## 런타임 미검증 (Supabase 미연결)
- RLS 실제 동작(lodgings 컬럼 grant, bookings_public 미러 트리거, journal anon select, team_members display_name 컬럼 grant)
- Realtime 구독·5초 반영(SC-004), magic link 발송(I-007), Resend security 알림
- 연결 후 절차: `docs/04-report/booking-tracker.report.md` "Supabase 연결 시 해야 할 일" 참조

## 재실행 (2026-09-18, 프로덕션 빌드 기준)
| 단계 | 명령 | 결과 |
|---|---|---|
| L1 타입 | `npx tsc --noEmit` | 0 오류 |
| L1 단위 | `npx vitest run` | 14 files · 115/115 통과 |
| L2/L3 E2E | `npx playwright test --workers=1` — webServer `npm run build && npm run start -- -p 3100`, `NEXT_DIST_DIR=.next-e2e` | **99/99 통과 (58.5s)** |

### 이번 재실행에서 잡은 결함 2건
| 결함 | 원인 | 조치 |
|---|---|---|
| dev 서버에서 SW가 옛 청크를 서빙해 관리자 탭·홈 앱 오류 | SW가 해시 없는 dev 자산을 캐시(offline-pwa Act-2) | SW는 프로덕션에서만 등록, dev는 SW·캐시 자동 제거, E2E는 프로덕션 빌드로 전환 |
| axe `aria-prohibited-attr`(serious) 간헐 실패 4~7건 | `CardSkeleton` 래퍼가 role 없이 `aria-label` 사용. 프로덕션 스트리밍 중 skeleton이 스캔에 잡힐 때만 노출 | `src/components/ui/Skeleton.tsx`에 `role="status"` 추가 |

## PRD v3.0 델타 재실행 (2026-09-18)
| 단계 | 명령 | 결과 |
|---|---|---|
| L1 타입 | `npx tsc --noEmit` | 0 오류 |
| L1 단위 | `npx vitest run` | 15 files · 124/124 통과 (SC-015 8/16 케이블카, D-1 10항목, Day 12 숙박 문구, GPX 12개) |
| L2/L3 E2E | `npx playwright test --workers=1` (프로덕션 빌드 3100) | 105/108 → 8/16 케이스 셀렉터를 `leg-timeline` testid로 한정 후 재실행 9/9 통과 → **108/108** |
| 게이트 | itinerary-core 100%(Act-1) · budget-view 97.9% · day-detail 100%(Act-1, 문서 보정) | 통과 |

## PRD v3.1 델타 재실행 (2026-09-18, D-007)
| 단계 | 명령 | 결과 |
|---|---|---|
| L1 타입 | `npx tsc --noEmit` | 0 오류 |
| L1 단위 | `npx vitest run` | 15 files · 126/126 통과 (8/16 2구간·8/17 3구간, chamonix-hotel-2 seed↔SQL, D-1 275/390) |
| L2/L3 E2E | `npx playwright test --workers=1` (프로덕션 빌드 3100) | **111/111 통과** · Act-1 후 8/16·8/17 케이스 재실행 2/2 |
| 게이트 | itinerary-core 100%(Act-1: 케이블카 fallback v3.1 문구·8/17 상태) · booking-tracker 96.9% · budget-view 98.1% | 통과 |

## v3.1 보정 (2026-09-18, 외부 사실 실측 반영)
열차 시각(10:02→13:05, 직통 IC1 30분 간격)·에귀 뒤 미디 운행/요금(06:10~18:00, €60.20~83)·준비물 카테고리명 반영 후: `tsc` 0 · vitest 126/126 · 프로덕션 E2E(예산·8/16·8/17·준비물) 통과.
